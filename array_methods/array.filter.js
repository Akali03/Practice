const numbers = [1, 2, 3, 4, 5];

const even = numbers.filter(num => num % 2 === 0); //Create a new array with elements passing a condition
console.log(even);

const compare = numbers.filter(num => num === 3)
console.log(compare);
